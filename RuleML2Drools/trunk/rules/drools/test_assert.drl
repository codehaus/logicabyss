package ruleml.translator
import ruleml.translator.TestDataModel.*;

rule "test1"
when
    $p:Person( name == "Tisho", age < 31 )
	$b:Buy(buyer == $p, $s:seller, $i:item=="Amazon")
	Keep(keeper == $p, item == $i)
then
	insert( new Own($i, $p));
end